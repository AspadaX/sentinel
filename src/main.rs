/*
    This program intends to do the following:
    - Check whether a given code file suits the specified business logics
    - If it does not, the checker will report errors
*/
fn main() {
    println!("Hello, world!");
}
